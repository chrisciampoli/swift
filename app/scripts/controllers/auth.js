'use strict';

/**
 * @ngdoc function
 * @name swiftApp.controller:AuthCtrl
 * @description
 * # AuthCtrl
 * Controller of the swiftApp
 */
angular.module('swiftApp')
  .controller('AuthCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
